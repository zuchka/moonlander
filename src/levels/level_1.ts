export const level1Config = {
  landingPad: {
    // Define initial properties, e.g., position, size
    width: 80,
    // Example: Place it roughly in the center for now
    // We'll adjust how this is used later
    xPositionFactor: 0.5, // 0.5 means center
  },
  lander: {
    initialFuel: 100,
    // Add other lander initial state if needed
  },
  terrain: {
    // Define terrain generation parameters
    // e.g., roughness, segment count, etc.
    // We'll keep this simple for now
  },
  // Add other level-specific settings
}; 